# 通行证优化任务 — 进度与计划（2026-06-17）

> 生效副本：`server-csharp-modded`（AccountWeb 为旧快照，不改）。
> 用户页：`SPT_Data/battlepass/page/`；后台：`page/admin/`；服务端：`Core/BattlePass/`。

## 七项任务核对结论（动手前已逐项检查）

| # | 任务 | 初始状态 | 处理 |
|---|------|---------|------|
| 1 | 奖励轨已解锁/未解锁亮度区分 | ❌ 未做。`.cell.locked` 仅 opacity .38，已解锁格用暗 panel 背景不够突出 | **要做**：已解锁=强高亮+发光，未解锁保持暗淡 |
| 2 | 后台价格改货币下拉 | 🟡 基本完成。`BpPrice`(price.js) 已建，trader.js/items.js 接入；flea.js/recipes.js 不涉货币 | 收尾核查无残留手填 |
| 3 | 到达地点/安放物品区域ID下拉 | ✅ 已做。`zoneSelect()` 用 datalist（可选可手填），PlaceItem/VisitZone 都用 | 用户确认保持现状，**完成** |
| 4 | 任务配额/刷新周期可配 + 主界面滚动 | 🟡 部分。服务端模型(`*TaskCount`/`TaskDisplayCount`/`*PeriodHours`)全有；后台刷新UI有；但配置入口未暴露、用户页仅"刷新每日"、`TaskDisplayCount` 前端未用 | **要做**：见下方设计 |
| 5 | 激活码框加长/按钮缩小 | ❌ 未做。`.redeem input{flex:1}` 但侧栏仅 348px | **要做**：纯 CSS |
| 6 | 服务端追踪安放/到达 | ✅ 已做完。结论：服务端档案无此二维度，已配合客户端 mod `SPT-BattlePass-Client`(RaidTracker 上报)，端到端闭环 | **完成** |
| 7 | 上交入口+FIR | ✅ 已做完。网页上交流程(script.js:332-384) + FIR复选框(`findInRaid`)齐全 | **完成** |

## 任务4 设计（用户授权"由我决定合理默认"）

- **用户分别刷新三类任务**：新增管理员可配的「每 scope 免费刷新次数」存 `BpSeason`：
  `dailyRefreshLimit`(默认1) / `weeklyRefreshLimit`(默认1) / `seasonRefreshLimit`(默认0)。
- 用量存 `BpProgress`：复用 `DailyRerollsUsed`，新增 `WeeklyRefreshUsed` / `SeasonRefreshUsed`，按各自周期滚动时重置。
- `/tasks/refresh` 扩展为三 scope 各自 gate；`/tasks` GET 返回三类剩余次数。
- 用户页：「刷新每日 / 每周 / 赛季」三按钮，各显剩余；某 scope 无活跃任务或上限0 则禁用。
- 后台赛季页新增「任务系统设置」面板：`*TaskCount` / `taskDisplayCount` / `*PeriodHours` / `*RefreshLimit`。
- 用户页任务区用 `taskDisplayCount` 限定可视高度（恰好 N 张卡），其余滚轮/滑块滚动查看。

## 执行进度

- [x] 调研与核对（记忆过时，多项已完成）
- [x] #1 奖励轨配色（强高亮+发光）— script.js 加 `claimable` 类 + style.css 发光动画（免费黄铜/付费熔岩橙，含 reduced-motion 兜底）
- [x] #5 激活码框布局 — `.redeem` 输入框 flex 放大、兑换按钮缩小
- [x] #4 配额/刷新/滚动 — 服务端：`BpSeason` 加 `*RefreshLimit`、`BpProgress` 加 `Weekly/SeasonRefreshUsed`、`RollScope` 周期重置、`/tasks/refresh` 三 scope 化、`/tasks` 返回 `refreshLeft`+`displayCount`、`Reroll` 改用 `season.DailyRefreshLimit`、删 `FreeDailyRerolls` 常量；后台：tasks.html/js 加「任务系统设置」面板（10 字段写赛季）；用户页：三刷新按钮 + `displayCount` 限定可视任务数滚动。**Core 编译 0 error。**
- [x] #2 收尾核查 — trader.js/items.js 货币均走 BpPrice 下拉，无残留手填（flea/recipes 不涉货币）
- [x] #8 跳蚤黑名单：展示有效全量（含 BSG）+ 可移除/放开（详见下）
- [x] #9 物品管控页快捷加入跳蚤黑名单（详见下）

**全部 9 项完成；Core 全量编译 0 error。前端为 html/js/css，无需编译，未做实机验证。**

### 追加：奖励轨展示名优先级健壮性（2026-06-17）

- 需求：用户侧奖励轨详情按 **展示名 → 物品名 → MongoID** 逐级回落。
- 核查：`BattlePassController.SanitizeRewards`（覆盖 levels + cycle，未提交）已实现该解析（空展示名→`ItemSearchService.ResolveItemName`），后台编辑器空展示名存 `name:null`，逻辑本就正确——运行端若仍显示 MongoID，多为**旧构建未含此未提交代码，需重建 server-csharp-modded 并重启**。
- 健壮性补强：把"展示名缺失"判定从"仅空"扩展为 **空 / 等于 tpl / 本身是个 MongoID**（兼容旧数据把 id 当名字存的情况），命中即解析物品名，解析不到清空 Name 交前端回退到 tpl。Core 编译 0 error。

## 追加任务（会话中新增）

- **#8**：后台跳蚤黑名单展示有效全量并可移除 — **已完成**（用户选"展示有效全量黑名单"）。
  - 后端：`GET /flea/blacklist/effective` 枚举真实物品，`blacklist.Custom`∪(`EnableBsgList`&`CanSellOnRagfair==false`) 计入，标注 managed/custom/bsg，counts + BSG 截断(1500)；`POST /flea/whitelist/toggle` { tpl, add } 放开。控制器注入 DatabaseService/ConfigServer/ItemBaselineService。
  - 前端：flea 页新增"有效全量黑名单"面板（汇总计数 + 筛选 + 列表，每项来源徽章 + 放开按钮）。放开：managed→移出 BlacklistTpls；custom/bsg→加入 WhitelistTpls 强制放开。立即生效并同步本地 CFG。Core 编译 0 error。
- **#9**：物品管控页快捷加入跳蚤黑名单 — **已完成**。
  - 后端：`POST /battlepass/api/admin/flea/blacklist/toggle` { tpl, add? } 增量增删 + 即时 Apply。
  - 前端：items 页获取图谱头部加"加入/移出跳蚤黑名单"按钮，操作后刷新状态。Core 编译 0 error。
