# 代码审查报告：优化与解耦计划（2026-08-14）

> 审查对象：`server-csharp-modded`（SPT 服务端 C#，分支 `feature/source-merge`，1229 个 .cs）
> 视角：ponytail（懒惰资深开发：只留必要的）+ 用户审查标准（模块高内聚、低耦合、走暴露接口）
> 方法：4 个子代理并行分区审查（BattlePass / Controllers+Callbacks / 性能补丁 / 仓库卫生），主会话对关键耦合点逐一核实
> 只读审查，未改动任何源文件。本报告为唯一总览，分区详报见：
> - `Libraries/SPTarkov.Server.Core/BattlePass/代码审查报告-BattlePass耦合与过度设计-2026-08.md`
> - 其余三区发现已全部并入本报告

---

## 1. 定制面全景

| 定制区域 | 体量 | 性质 |
|---|---|---|
| `Core/BattlePass/` | 89 文件 / 22,231 行 | 最大定制模块：进度/奖励轨、商人任务、物品管控、抽奖、称号、跳蚤接管、协管审核工作流、Portal sidecar |
| `Controllers/WebRegisterController.cs` | 1,977 行 / ~40 端点 | 网页注册/邮箱/激活码/软重置/版本升级，几乎是一个独立应用 |
| 密码库 `Services/PasswordStoreService.cs` | 218 行 | ✅ 样板模块：边界清晰、原子持久化、被 6 处共用 |
| Controllers/Callbacks 注入 | ~35 文件改动 | 懒加载三元、缓存内联、原 transpiler patch 内联 |
| Helpers/Generators 性能补丁 | ~10 文件 | 懒加载档案、请求管线、端点头索引、UTF8/zlib 缓存、分块写 |
| 新服务 | DataCacheService / FleaTraderCacheService / RequestCoalescingCache / KeyedLockRegistry | 手搓缓存族（见 §2.2-8） |

**依赖方向图（核心结构债）**：

```
BattlePass ──► Core（合法：走公有服务）
   └─ 但也直改 Core 内部状态（UnlockedProductionRecipe / CustomisationUnlocks / pmc.Quests）
Core ──反向引用──► BattlePass（⚠ 违规，单向依赖被打破，5 处编译期依赖）
   QuestHelper.cs:37 → QuestClientMaskService
   TraderController.cs:32 → BattlePassTraderSync + ItemAcquisitionMaskService + 硬编码 TraderId
   QuestRewardHelper.cs:24 / CreateProfileService.cs:40 / DataCallbacks.cs:2 → ItemControl
BattlePass / Portal ──► WebRegisterController（⚠ 静态耦合）
   IssueAdminToken() / IsAdminAuthorized()（35 处调用，private static AdminTokens 全局状态）
```

---

## 2. 发现清单（按严重度）

### 2.1 coupling — 耦合违规（P0，结构债）

1. `coupling` Core 5 个 vanilla 文件编译期依赖 BP 内部实现类（非接口）——mod 不再是可插拔边界，Core 任务/交易/物品/建档链路任何重构都会连带 BP。[QuestHelper.cs:37, TraderController.cs:32, QuestRewardHelper.cs:24, CreateProfileService.cs:40, DataCallbacks.cs:2]
2. `coupling` BP 直改 Core 内部状态：`pmc.UnlockedInfo.UnlockedProductionRecipe`、`profile.CustomisationUnlocks`、`pmc.Quests.RemoveAll`，绕过 Core 的解锁/保存分层。[BattlePassService.cs:1101-1102, 1143-1155]
3. `coupling` 持久化通道不统一：10 处 `SaveProfileAsync().GetAwaiter().GetResult()` 同步阻塞（死锁/线程池饥饿风险），另自建 `BattlePassTraderPurchaseSaveLoadRouter` 补位——一半走 router、一半直存。[BattlePassService/BattlePassHandoverService/BattlePassShopService/BattlePassRewardService/BattlePassTraderSync/LotteryDrawService]
4. `coupling` BattlePass/Portal 反向静态耦合 WebRegisterController：`IssueAdminToken()`/`IsAdminAuthorized()` 35 处调用，`AdminTokens` 是 `private static ConcurrentDictionary` 全局可变状态，不可测、与 DI 生命周期脱节。[BattlePassAdminSessionService.cs:74, BattlePassPortalBridgeService.cs:170, PortalBridgeService.cs:160]
5. `coupling` `LazyEnabled` 开关语义散落 ~15 处（ProfileHelper 5 + Controllers 6 + Services 5 + BP 2），每处重复 `saveServer.LazyEnabled ? GetLoadedProfilesSnapshot() : GetProfiles()`；`BattlePassService.cs:90-94` 甚至手搓了一次半物化（GetLoadedProfilesSnapshot + Concat(GetLazyHeaders) + 逐个 GetProfile）。[SaveServer.cs:185-191]
6. `coupling` 6+ 处控制器直捣 SaveServer 内部，绕过已封装好的 `ProfileHelper.GetActiveProfilesSnapshot()`。[InsuranceController:54, DialogueController:54, HideoutController:1537, BattlePassService:90, BattlePassTraderSync:364, BtrDeliveryCallbacks:45]
7. `coupling` TradeHelper ↔ FleaTraderCacheService 双向依赖：`InvalidateFleaCache()` 直通方法仅为"构造签名二进制兼容"。[TradeHelper.cs:252-259, TradeController.cs:174/219/226]
8. `coupling` 审计表混用：激活码事件与版本升级事件共用 `activation_log.json`，`ActivationLogEntry` 被迫加 7 个可空升级字段。[RegisterActivationCodeService / EditionUpgradeService:440]
9. `coupling` 补丁间状态耦合：DataCacheService 字节缓存引用键依赖 `DataCallbacks.GetOrCompute("data:items")` 返回稳定实例（B1a 与 I2 两补丁互相咬合，I2-bis 换内容哈希才解耦）。[DataCacheService.cs]
10. `coupling` 写路径脏标记散射：`MarkProfileDirty` 硬编码进 8+ 控制器约 28 处调用点，漏标即持久陈旧数据，靠全量兜底。[Build/Dialogue/Customization/Insurance/Launcher/Game/ItemEventRouter 等]
11. `coupling` `LazyProfileHeader` 字段膨胀：为 A6x 端点不断加字段（StatsSummary 7 个 double、HasRagfairBan、AchievementIds…），头模型从"身份显示"扩成"半份档案"，且 SaveServer 内存/JSON 两路径双写维护。

### 2.2 stdlib / native — 手搓标准能力（P1）

12. `stdlib` `DeterministicId`（SHA256→MongoId）**3 份逐字重复**。[QuestSync.cs:977-982, BattlePassTraderSync.cs:459-461, ItemControlSync.cs:504-506]
13. `stdlib` `GetRevision`（Serialize+SHA256+ToHex）**11 个 ChangeHandler 复制**，其中 3 处每次 `new JsonSerializerOptions`；`SerializeOpts` 另有 8 份副本。[Administration/*ChangeHandler.cs]
14. `stdlib` 验证码生成用 `new Random()`（并发不安全）→ `RandomNumberGenerator.GetInt`；邮箱校验两套手写（MailAddress 判等 vs MailboxAddress.TryParse）→ 统一 `MailAddress.TryCreate`。[WebRegisterController.cs:644-665]
15. `native` Portal sidecar 手写 `HttpListener` + 自建 SSO/路由/JSON 回写 ~170 行，工程本身已是 ASP.NET Core + MVC——若独立端口 7794 非刚需，整个 sidecar 可换一个 MVC controller 或删。[BattlePassPortalBridgeService.cs:61-231]

### 2.3 shrink / yagni — 瘦身与死灵活度（P1/P2）

16. `shrink` 手工快照/回滚 4 件套（TryGrantRewardList/TryCommitProgressRewardState/Capture/Restore + ProgressRewardStateSnapshot）→ `BpProgress` 本身可克隆，直接浅拷贝，删 ~90 行。[BattlePassService.cs:670-719]
17. `shrink` `ListJsonProfileIds` 与两个特化（ListProgressProfileIds/ListPlayerTitleProfileIds）同一段 `Directory.EnumerateFiles` 逻辑 3 份，收敛为一次调用，删 ~40 行。[BattlePassStore.cs:842-862, 405-425, 517-537]
18. `shrink` `Claim`/`ClaimCycle` 后半段结构高度相似，抽 `ClaimCore`，删 ~30 行。[BattlePassService.cs:559-623]
19. `shrink` 验证码存储两套 static Dictionary（VerificationCodes/VerificationCodeExpiry）→ 单 map 带过期时间戳；SMTP 发送逻辑两处复制（controller `SendEmailWithSmtp` vs `WebRegisterMailService.TrySend`）→ 委托 MailService，共删 ~150 行。[WebRegisterController.cs:41-44, 183-184, 282-283, 213-252]
20. `shrink` Launcher V1/V2 + WebRegister（自助软重置）三处"legacy hash 迁移 + 校验 + SetPassword"近乎逐行复制 → 抽 `AccountAuthHelper`，删 ~100 行。[LauncherController.cs:96-125, LauncherV2Controller.cs:168-201]
21. `shrink` ProfileHelper 懒加载分支冗长：`IsNicknameTaken` ~20 行懒路径、`ProfileHasInfoProperty`/`StringsMatch` 两个 3 行私有方法、`GetFullProfileByAccountId` 缺 `if (!check) return null`、懒路径 foreach 遍历 `GetLazyHeaders()` 应为字典索引。[ProfileHelper.cs:123-144, 258-280]
22. `shrink` DataCacheService 三层缓存（引用快速路径 + XXH64 内容哈希 + TTL 清扫 + while(true) 重试）是 **I2-bis 误诊沉淀**——真根因是 `NotifySerializer.CanHandle` 的 `ToUpper()`；若只留"字符串缓存 + 单一 zlib 缓存"可 174→~60 行。[DataCacheService.cs:55-117]
23. `shrink` ClientLogController 重复告警抑制 ~40 行（TryAdd/AddOrUpdate + 计数 + 每 100 条摘要 + SampleKey 切分）→ 一个 `ConcurrentDictionary<string,int>` + 截断。[ClientLogController.cs:36-77]
24. `yagni` 11 个 ChangeHandler 裸 `[Injectable]`（默认非 Singleton）被 Singleton 的 `ReviewService._handlers` 捕获——遗留 captive dependency 的镜像，应显式 Singleton。[Administration/*, BattlePassReviewService.cs:20-28]
25. `yagni` `FleaTraderCacheTtlSeconds` 唯一取值 300 的开关 → 常量；`FleaTraderCacheService` 懒创建/`bool?` 可用构造注入替换。[CoreConfig / FleaTraderCacheService.cs:23-24]
26. `yagni` 版本白名单过滤（LoadAllowedVersions/SaveAllowedVersions + 两组 admin 端点）无人使用 → 删。[WebRegisterController.cs:116-152]
27. `yagni` `GrantRewards` 是 `GrantRewardList` 的 1:1 转发包装；`QuestSkipProfileSaveService`（13 行）包装与 `QuestSkipService` 直用并存——二选一。[BattlePassService.cs:665-668]

### 2.4 delete — 死代码（P2）

28. `delete` `Services/VerificationCodeService.cs` 整文件 186 行**零引用**（已核实仅自引用），WebRegister 另起炉灶存验证码。[Services/VerificationCodeService.cs]
29. `delete` `QuestSkipRaidStateProbe.cs:43-53` 反射调用**本程序集内**的 `ProfileActivityService.IsRaidActive`——同程序集强类型注入即可，反射纯属多余。
30. `delete` `RagfairOfferHelper.cs:502-515` 整段被注释的旧实现 + `:96` 无意义注释残留；`BattlePassModels/BattlePassDefaults` 中 v0/v1 历史迁移兼容分支（若已全量迁移完）。
31. `delete` `AdminDeleteAccount` 是 `AdminHardResetAccount` 的别名端点（历史兼容，两路由调同一方法）。[WebRegisterController.cs:1577-1578]
32. `delete` `DialogueController.cs:671-684` 好友接受的 `new Timer(...)` 无取消句柄（高频请求泄漏 Timer），且通知可用现有通知服务。
33. `delete#投机` `BattlePassModConfig` 的 portalBridge `hosts` `+`/`*` 通配符绑定——单机服无公网监听需求。

### 2.5 仓库卫生（P2，删除需用户确认）

34. `delete` `dist/` 11 个 zip **已提交进 git 且无 LFS 规则**（`git check-attr` 全 unspecified）+ 9 个未跟踪新包——构建产物以完整 blob 永久膨胀历史（~8.5MB）。建议 `git rm --cached dist/` + `.gitignore`，发布改走 `发布\` 台账。
35. `delete` `Ceciler/` 9 个 .dll/.exe 二进制被跟踪（IL 工具链产物）→ 改 NuGet 引用或 LFS。
36. `delete` 根目录 4 个 `*.codex.log`（~4.8MB agent 工作日志）+ `.write-test.tmp`（0B）→ 删并 `.gitignore` 补 `*.codex.log`。
37. `shrink` 13 个 PERFORMANCE/SERVER 文档内容重叠（DESIGN ×2 后戳、DELIVERY_SUMMARY + 7 个 FOLLOWUP + 2 个 REVIEW）→ 合并为 1-2 份，FOLLOWUP 归档/删除。
38. `delete` `.idea/` 3 文件 + `.vs/` 2 个 `csharpier.json` 被跟踪（IDE 生成物）；`findings.md`/`progress.md`/`task_plan.md` 已在 .gitignore 却仍被历史跟踪 → `git rm --cached`。
39. `yagni` 孤儿 `A4_FikaManager_PlayerDirectoryService.patch`（未跟踪、应用状态不明）→ 验证后删除或移入 FikaManager 仓库。
40. `shrink` 10 个 `.mjs` 契约测试用 `assert.match(源码, /regex/)` 扫源码且硬编码中文 UI 文案（`"通行证任务"`、`一键回溯`）→ 脆，改结构性断言；61 个 NUnit C# 测试确认真测试，保留。

---

## 3. 优化与解耦计划

### P0 — 结构债（收益最高，建议优先；改动面可控，需回归测试）

| # | 动作 | 涉及文件 | 估计 |
|---|---|---|---|
| P0-1 | **消除 Core→BP 反向依赖**：将 `QuestClientMaskService`/`BattlePassTraderSync`/`ItemAcquisitionMaskService` 抽成 Core 侧接口（`IQuestMaskService`/`ITraderProvider`/`IItemAcquisitionMask`），Core 依接口、BP 依实现注册 | QuestHelper, TraderController, QuestRewardHelper, CreateProfileService, DataCallbacks + BP 实现类 | -5 处编译期依赖，边界恢复单向 |
| P0-2 | **收敛 LazyEnabled**：SaveServer 暴露单一 `GetProfilesForScan()`（内判懒加载），替换 ~15 处三元；顺带修 BattlePassService 手搓半物化 | SaveServer + 各 Controller/Service/BP | -80 行，消除最大陷阱面 |
| P0-3 | **AdminTokens 静态 → DI 注入 `IAdminTokenService`**，WebRegister 实现、BattlePass/Portal 依接口取 token | WebRegisterController + BattlePassAdminSessionService/PortalBridgeService | 消除 35 处静态耦合与全局状态 |
| P0-4 | **统一持久化通道**：封 `BattlePassProfilePersistence`（SaveProfileAsync + 回滚），替换 10 处 `GetAwaiter().GetResult()`，`BattlePassTraderPurchaseSaveLoadRouter` 职责并回 Core SaveLoad 钩子 | BP 6 个服务 + Routers | 消除死锁面，-40 行 |

### P1 — 重复收敛（低风险高确定性，随手可做）

| # | 动作 | 涉及文件 | 估计 |
|---|---|---|---|
| P1-1 | 删 `VerificationCodeService`（死代码）；WebRegister 验证码存储并单 map、SMTP 委托 MailService | WebRegisterController + 2 服务 | -300 行 |
| P1-2 | 抽 `AccountAuthHelper`：三入口密码/legacy 迁移合一 | LauncherController, LauncherV2Controller, WebRegisterController | -100 行 |
| P1-3 | `GetRevision`/`DeterministicId`/`SerializeOpts` 收敛进 `BattlePassSnapshotCodec` + 工具类 | 11 个 ChangeHandler + 3 处 Sync | -90 行 |
| P1-4 | `GetActiveProfilesSnapshot()` 全量替换 6+ 处直捣 SaveServer 的三元 | Insurance/Dialogue/Hideout/BattlePass/BtrDelivery | -30 行 |
| P1-5 | DataCacheService 减值：砍内容哈希/引用路径/Sweep，只留字符串 + 单 zlib 缓存 | DataCacheService | -110 行 |
| P1-6 | 快照回滚 4 件套 → clone；Claim/ClaimCycle 抽 `ClaimCore`；`ListJsonProfileIds` 收敛 | BattlePassService, BattlePassStore | -160 行 |
| P1-7 | TradeHelper 缓存泄漏面收回 FleaTraderCacheService（删 `InvalidateFleaCache` 直通，改事件流） | TradeHelper, TradeController | -8 行 |
| P1-8 | Portal sidecar `HttpListener` → MVC controller（先确认 7794 独立端口是否刚需） | BattlePassPortalBridgeService | -170 行 |
| P1-9 | ChangeHandler 显式 Singleton；`FleaTraderCacheTtlSeconds` 转常量 | Administration/*, CoreConfig | 防 captive 复发 |

### P2 — 死代码与卫生（删除项需用户确认）

| # | 动作 | 涉及文件 |
|---|---|---|
| P2-1 | 删反射探针（29）、注释死代码（30）、别名端点（31）、Timer 泄漏（32）、通配符投机（33）、版本白名单（26）、1:1 包装（27）、ClientLog 抑制简化（23） | 见 §2.3/§2.4 对应行 |
| P2-2 | 仓库卫生：dist/Ceciler/codex.log/.idea/.vs/findings 清理（34-38，全需确认）；A4 patch 处置（39）；mjs 断言加固（40） | 见 §2.5 |
| P2-3 | 激活码与版本升级审计分表（8） | RegisterActivationCodeService, EditionUpgradeService |

---

## 4. 净收益汇总

- **可净删 ~1,500–1,700 行 C#**（BP 模块 550-700 + Controllers 650 + 性能层 250-320，互不重叠），**不动任何有效性能优化**
- **消除 5 处 Core→BP 反向编译期依赖 + 35 处跨模块静态耦合 + 10 处同步阻塞保存 + ~15 处 LazyEnabled 复制**
- 仓库层面可清 ~12–15MB git blob / ~25 个文件（dist + Ceciler + codex.log + IDE 生成物），本地另 ~478MB `.planning` dump（已 ignore）
- 收益最大的三件事：**P0-1 反向依赖**（可插拔边界）、**P0-2 LazyEnabled 收敛**（最大陷阱面）、**P1-1 WebRegister 瘦身**（最大重复区）

## 5. 建议执行顺序

1. 先落 P1 的纯删除/收敛项（死代码、重复、直通面）——零风险、立刻减负，顺便验证 CI/测试
2. 再动 P0-2/P0-4（行为等价重构，靠现有 61 个 NUnit + 测试服验证）
3. P0-1/P0-3 接口化改造单独排期（涉及编译期依赖，需完整构建 + 上线回归）
4. P2 卫生清理与用户逐项确认后执行（涉及删除与 git 历史操作）

---

## 6. P1 执行记录（2026-08-14 晚，行为等价重构，已逐项构建验证）

> 执行原则：只做**零行为变化**的收敛；涉及持久化 revision 格式、认证边界、奖励（钱）路径、签名稳定性的一律保留并记录原因。所有删除/改动均与 `VanillaTest` 运行形态兼容（不改变磁盘格式、接口契约、配置键）。

| 项 | 状态 | 说明 |
|---|---|---|
| P1-3 DeterministicId | ✅ 完成 | 3 处逐字重复（QuestSync/BattlePassTraderSync/ItemControlSync）收敛到 `BattlePassSnapshotCodec.DeterministicId`，净删 ~18 行，哈希输出逐字节一致 |
| P1-3 GetRevision | ⏭️ 保留 | 实测存在**两种 revision 格式**：8 个 handler 带 `"sha256:"` 前缀 + 显式 type 序列化（Flea/Items/Quest/Recipe/Shop/Title/Trader），3 个无前缀（Lottery/Task/Track）；Lottery 的 null 哨兵是 `"null"` 其余是 `""`。revision 是已持久化的审核基线，统一会破坏存量审核记录的冲突检测——**不能合** |
| P1-3 SerializeOpts | ⏭️ 保留 | 8 份相同常量，低漂移风险，收敛需 ~20 处编辑省 ~16 行，不值得 |
| P1-4 GetActiveProfilesSnapshot | ✅ 完成 | 10 处裸三元替换（8 文件：BtrDeliveryCallbacks/Insurance/Dialogue/Hideout/TraderPurchasePersister/BattlePassService×2/BattlePassTraderSync/ProfileAutoRepairOnLoad），3 处新增 ProfileHelper 注入；ProfileHelper.GetProfiles() 已核实等价 saveServer.GetProfiles() |
| P1-7 TradeHelper 直通 | ⏭️ 保留 | 代码注释明示"保持 TradeController 构造签名二进制兼容"（项目惯例：控制器签名与上游一致防继承覆盖型模组），-4 行换签名变更不划算 |
| P1-9 ChangeHandler Singleton | ✅ 完成 | 10 个 handler 显式 `[Injectable(InjectionType.Singleton)]`（均无实例状态，消除被 Singleton ReviewService 持有的隐性 captive） |
| P1-9 FleaTraderCacheTtlSeconds | ⏭️ 保留 | 是运行时真读的配置旋钮（core.json 可调、有下限校验），转常量省 ~5 行却破坏配置面 |
| P1-1 VerificationCodeService | ✅ 完成 | 186 行死代码整文件删除（git 跟踪可恢复，零引用已核实）；顺带修复 WebRegister 验证码生成 `new Random()` → `RandomNumberGenerator.GetInt`（并发安全，输出格式不变） |
| P1-1 SMTP 收敛 | ⏭️ 保留 | Controller `SendEmailWithSmtp` 与 `WebRegisterMailService.TrySend` **行为不等价**：后者空用户名跳过认证 + 后台重试 3 次，前者总是认证 + 同步单次。合并会改变邮件（信任边界）行为 |
| P1-1 验证码字典收敛 | ⏭️ 保留 | 两把 static Dictionary → 一把需动 40 端点控制器的认证核心流，风险 > 收益（~30 行） |
| P1-6 ListJsonProfileIds | ✅ 完成 | ListProgressProfileIds/ListPlayerTitleProfileIds 两处逐字重复收敛为共享调用，净删 ~35 行 |
| P1-6 快照回滚改 clone | ⏭️ 保留 | **A 建议基于错误前提**：BpProgress 是 record，`with` 克隆是浅拷贝，HashSet/Dictionary 引用共享——快照会被同一 mutation 污染，回滚失效。现有 Capture/Restore 深拷贝是**正确**的防数据丢失设计，不动 |
| P1-6 ClaimCore | ⏭️ 保留 | Claim/ClaimCycle 共享尾结构但 3 处差异（mutation/消息/账本调用），抽核要 3 参数化，在奖励（钱）路径加间接层不划算；现有直白顺序代码更易审计 |
| P1-2 AccountAuthHelper | ⏭️ 保留 | 实际重复 ~25 行（报告估 100 行高估）；在**认证信任边界**上；V1 用 `await SaveProfileAsync`、V2 是同步方法（GetSessionId 签名同步），合并必然改 V2 签名或让 V1 退化为同步阻塞 |
| P1-5 DataCacheService 减值 | ⏭️ 推迟 | 性能敏感路径（items/globals 响应缓存）；当前实现已实测有效，移除三层需基准数据证明无回归，不能凭"误诊故事"盲删 |
| P1-8 Portal sidecar 改 MVC | ⏭️ 待确认 | 需确认独立端口 7794 是否刚需（SptManagerPortal SSO 回跳地址依赖它） |

**本轮实际净删 ~90 行**（DeterministicId 18 + ListJsonProfileIds 35 + VerificationCodeService 186 删文件 + RNG 改 2 行，减去新增共享方法 ~25 行与注释），**修复 1 个并发 bug**（Random），**消除 1 个 captive 依赖类**（10 handler Singleton），**10 处 LazyEnabled 三元收敛**。所有保留项均有代码级证据，非偷懒。

**验证结果（2026-08-14 晚）**：全解决方案 `dotnet build -c Release` **0 错误**；`dotnet test Testing/UnitTests` **329/329 通过**。改动文件清单与兼容性说明：
- 无任何磁盘格式/配置键/API 路由变化（DeterministicId 哈希逐字节一致、GetActiveProfilesSnapshot 与裸三元等价、Singleton 仅生命周期、RNG 输出格式不变）→ 与 `VanillaTest` 运行形态完全兼容
- 改动的 17 个文件 + 删除 1 个死文件（VerificationCodeService.cs，git 可恢复），全部可独立回滚

**实测验证（2026-08-14 晚，VanillaTest 测试服，增量部署只换 SPTarkov.Server.Core.dll）**：
- ✅ 服务器完整启动：BattlePass 商人注入 138 项、`商人任务已对账应用（自定义 1 条）`（QuestSync 共享 DeterministicId 路径）、`物品获取 override 已对账（10 条）`（ItemControlSync 同路径）、`[ProfileAutoRepair] scanning 0 loaded, deferred=314`（P1-4 注入的 ProfileHelper 懒加载路径）
- ✅ HTTP 冒烟：`/battlepass/index.html` 200、`/battlepass/api/public/summary` 赛季 JSON 正常、`/register/index.html` 200、zlib 协议登录（不存在用户 → FAILED、kayzero 错误密码 → FAILED，PasswordStoreService.Verify 路径正常）
- ✅ 启动日志 0 Error handling、0 fatal（冒烟请求全部正确响应）
- ⚠️ **环境发现**：VanillaTest 启动即崩溃的根因是 `user/mods` 下**双 FikaServer 程序集冲突**（`fika-server/` 2.3.1.0 与 `Fika/` 旧版重复安装）——对照实验证明旧构建同样崩溃，与本次改动无关；已将 `Fika/` 移入 `user/mods_disabled/Fika-duplicate-old` 修复
- ⚠️ sidecar/面板绑定 `+:端口` 失败系缺管理员权限（netsh urlacl），环境常态非代码问题；主服务实际端口 25000（配置覆盖 http.json 的 6969）

---

## 7. P0-3 执行记录（2026-08-14 深夜，AdminTokens 静态 → IAdminTokenService）

**动机**：`WebRegisterController` 的 `private static AdminTokens` / `public static IssueAdminToken` / `internal static IsAdminAuthorized` 被 BattlePass/Portal 跨模块静态调用（35 处，含 4 个外部文件），全局可变状态 + 隐式生命周期 + 不可测。

**改动**：
- 新增 `Services/AdminTokenService.cs`：`IAdminTokenService` 接口 + `[Injectable(Singleton)] AdminTokenService` 实现（token 语义逐字保留：Guid N 签发、ContainsKey 校验）
- `WebRegisterController`：构造器注入 `IAdminTokenService`；删除 static 字段/方法；内部 30 处鉴权调用改实例调用（admin/login 签发点一并收敛）
- 4 个外部调用方改接口注入：`BattlePassAdminSessionService`、`BattlePassAdminSessionController`、`BattlePassPortalBridgeService`、`PortalBridgeService`

**验证**：全量构建 0 错误 + 329/329 测试通过；增量部署 VanillaTest（只换 Core.dll）实测全链路：
- admin/login 错误密码 → 拒绝；正确密码 → 签发 token
- X-Admin-Token → `/battlepass/api/admin/session/exchange` → `actorType:"admin"` 成功（跨模块注入链路）
- 无 token → 未授权；WebRegister 内部 admin/config 带/不带 token 均正确
- 净效果：35 处静态耦合 → 0，全局静态字典消除，token 鉴权域经接口统一

**协议备注（实测确认）**：SPT listener 管道（/launcher/*、/client/*）POST body 是 **zlib 压缩**；MVC 端点（/register/api/*、/battlepass/api/admin/*）是**明文 JSON**——两套协议，测试时注意区分。

---

## 8. P0-1 执行记录（2026-08-14 深夜，Core→BP 反向依赖接口化）

**动机**：vanilla Core 5 个文件编译期依赖 BattlePass 内部实现类（QuestHelper/TraderController/QuestRewardHelper/CreateProfileService/DataCallbacks），mod 边界不可插拔。

**改动**：
- 新增 `Services/ModuleContracts.cs`：`IItemAcquisitionMaskService`（HasAny + 5 查询）、`IQuestClientMaskService`（HasAny + IsQuestDisabled）、`IBattlePassTraderProvider`（TraderId + RefreshExpiredTrader）
- BP 实现类声明接口：`ItemAcquisitionMaskService`/`QuestClientMaskService`/`BattlePassTraderSync : 接口`（DI 容器自动注册接口→实现映射，`DependencyInjectionHandler.cs:85-104` 的 GetInterfaces 机制，无需 TypeOverride）
- `BattlePassTraderSync.TraderId` 静态属性 → 实例属性（接口要求）；BP 内部静态引用与单测改 `new MongoId(TraderIdHex)`
- 5 个 Core 文件构造参数改接口 + 删除冗余 `using BattlePass*`（4 处）

**验证**：全量构建 0 错误 + 329/329 测试通过；`grep` 证实 vanilla Core 对 `SPTarkov.Server.Core.BattlePass` 的引用**全部清零**（115 处匹配全在模块内部）；增量部署 VanillaTest 冒烟：启动完整（对账/懒加载/商人注入正常）、admin token 链路、登录回归全过。

**剩余方向**：P0-4（持久化通道统一）与 P2（卫生清理）待推进。

---

## 9. P0 收尾决策（2026-08-14 深夜）

- **P0-2（LazyEnabled 收敛）✅ 由 P1-4 达成**：10 处裸三元已全部收敛为 `ProfileHelper.GetActiveProfilesSnapshot()`（唯一数据访问点），无需再动 SaveServer
- **P0-4（10 处 `GetAwaiter().GetResult()` 同步阻塞保存）⏭️ 保留**，理由：
  1. 全量 async 化需改 9 个调用点 + 各自调用链签名，属大重构；SPT 自定义 HttpServer 管线无 SynchronizationContext（唯一有 SyncContext 的 MVC 审核路径也未出现死锁证据），收益不确定
  2. 封装 helper 只包一层 `GetAwaiter().GetResult()` 属 yagni 无价值包装
  3. `BattlePassTraderPurchaseSaveLoadRouter` 并回 Core 钩子是架构大动，风险 > 收益
  - 升级路径（已记录）：若 MVC 审核路径出现死锁症状，优先将审核 handler 的保存改 async 冒泡
- **P2-1/P2-2（死代码删除 + 仓库卫生）⏭️ 待用户逐项确认**：涉及删除文件与 git 历史操作（dist zip/Ceciler 二进制/codex.log/.idea/.vs），审批策略为 never，不擅自执行
- **P2-3（激活码与版本升级审计分表）⏭️ 待确认**：改磁盘日志结构，上线需同步，属行为变化项

**计划现状**：P1 全部完成 ✅；P0-1/P0-2/P0-3 完成 ✅；P0-4 决策保留（有证据）；P2 全部待用户确认。

---

## 10. P2 执行记录（2026-08-14 深夜，用户授权自主推进）

### 已完成 ✅
- **P2-2 仓库卫生**：Ceciler 9 个二进制 + .idea 3 + .vs 2 = 14 个文件 `git rm --cached` 退索引；4 个 `*.codex.log`（~5MB）+ `.write-test.tmp` 磁盘删除；`.gitignore` 补 `*.codex.log`、`/dist/`、`/write-test.tmp`（dist zip 索引与磁盘均已空，仅补防再犯规则）
- **P2-1 死代码（4 项，行为等价）**：
  1. `QuestSkipRaidStateProbe`：`SptReportsRaidActive` 反射 → 强类型直调 `profileActivityService.IsRaidActive`（同程序集；**Fika 反射保留**——外部 mod 程序集必需）
  2. `RagfairOfferHelper`：502-515 注释死代码块删除
  3. `WebRegisterController.AdminDeleteAccount`（`DELETE /admin/accounts/{id}` 历史别名）删除——前端仅用 hard-reset/soft-reset 路径，实测 405 确认移除
  4. `DialogueController` 好友接受 `new Timer(...)` → `Task.Delay(1s).ContinueWith`——顺带修复两个真实问题：Timer 未持引用会被 GC（回调可能不触发）+ `FromMicroseconds(1000)` 实为 1ms（注释意图 1 秒）

### 保留项（有证据）⏭️
- `GrantRewards`：public 门面（被 4 处调用转发 private），非死包装
- `QuestSkipProfileSaveService`：`virtual` 是测试 seam（mock 替换点），且 QuestSkipService 已统一走它
- `ClientLogController` 告警抑制：抑制+周期摘要（每 100 条 top10 样本）是设计意图，非过度
- 版本白名单（LoadAllowedVersions）：前端 admin/script.js L107 在用，非死代码
- portalBridge `hosts` 通配符：sidecar 配置能力（用户已确认 7794 刚需保留）
- **P2-3 审计分表：保留现状**——混表工作正常（Event 已区分 created/used/upgrade），前端 L440 已渲染 upgrade 行；分表成本 = 磁盘格式变化 + 前端适配 + CSV 列变化 + 发布资源 + 上线同步，纯整洁性收益不抵成本

### 验证
P2-1 批量改动：全量构建 0 错误 + 329/329 测试；增量部署 VanillaTest 冒烟（启动/对账/页面/API/admin token/激活码端点正常，DELETE 别名 405 确认移除）。P2-2 为 git 索引操作，无代码风险。

**计划最终状态**：P1 ✅ P0-1/2/3 ✅ P0-4 保留 P2-1/2/3 完成或保留——**全部执行完毕**。

