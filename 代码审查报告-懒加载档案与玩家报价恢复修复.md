# 懒加载档案与玩家报价恢复修复代码审查报告

日期：2026-07-19  
目标：SPT 4.0.13 自定义 Core  
结论：通过；修复了可能让玩家报价恢复全轮中断的懒加载链路。

## 审查范围

- `SaveServer` 的档案文件排序、lazy header/用户名索引建立、按需物化与重建。
- `ProfileCleanupService` 删除重复档案后对懒加载索引的同步。
- `RagfairOfferService.AddPlayerOffers()` 对有挂单档案的物化、恢复和失败隔离。
- 目标单元测试、Release 构建及 190 档案隔离启动证据。

## 根因与修复

1. 启动时 lazy header 先建立，随后重复档案清理可能删除 header 指向的旧文件；后续 `GetProfile()` 会按失效路径物化失败，离线玩家报价无法恢复。
2. 档案文件枚举顺序原先依赖文件系统；同一档案存在 MongoId/用户名双文件时，最终索引指向不稳定。现在固定 MongoId 文件优先、用户名文件随后，使用户名文件确定性覆盖。
3. 新增 `RebuildLazyProfileHeaders()`，在写锁内清理并重建 header、用户名索引和加载锁，再覆盖已物化档案的当前信息。重复档案确实删除后立即调用重建。
4. `EnsureMaterialized()` 只有在真实加载成功且缓存中存在档案后才标记 loaded，失败不会留下伪加载状态。
5. `AddPlayerOffers()` 改为每档独立恢复；单个坏档的加载或报价写入异常只计入匿名汇总并继续其他档案，完整扫描后设置本轮 loaded 标记，不再每个 ragfair tick 重复阻断全局更新。

## 内聚与耦合

- lazy header 的构建和并发保护仍由 `SaveServer` 单独负责；清理服务只在完成删除后调用公开重建入口，没有直接篡改索引内部字典。
- 玩家报价恢复的容错留在 `RagfairOfferService`，不把存档解析策略泄漏到 Mod 或 Controller。
- `ProfileCleanupService` 只负责重复文件生命周期，并通过 `SaveServer` 暴露的接口同步状态，模块间保持低耦合。
- warning 只汇总失败档案数量，不输出档案 ID、用户名或异常内容。

## 验证结果

- 目标 NUnit：2/2，通过：确定性文件优先级；加载失败/报价写入失败隔离并继续恢复。
- Core Release 构建：通过，0 errors；全项目 warning 为当前定制仓库既有警告。
- `dotnet format` 与 `git diff --check`：通过。
- 隔离启动扫描 190 个档案头成功，Mod 独立 updater 连续五轮无异常；样本全部保持 deferred 且没有活动报价，因此运行态没有触发实际玩家报价恢复或成交。

## 残余风险

- 本次增量 Core DLL 来自当前定制服务端工作树，包含该工作树既有累计功能；必须部署到同一套 4.0.13 定制服务端，不能当作官方 vanilla Core 的通用补丁。
- 文件系统或 JSON 本身持续损坏的档案仍会被跳过，但不会再阻断其他玩家报价。

未发现阻止配套发布的问题。

## 发布产物

- `SPT-Server-懒加载档案索引重建+玩家报价恢复容错-20260719-182803-incremental.zip`
- SHA-256：`A7654D08651821C10023DFC3FF767CCE2E7EC5F7959A90090673F33C421BADD7`
- 清单：`SPT/SPTarkov.Server.Core.dll`
- 安装顺序：停止服务端后先覆盖本 Core 包，再覆盖同时间 DynamicFleaPrice 3.4.0 包。
